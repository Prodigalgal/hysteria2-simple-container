# .env.example
# 请将此文件复制为 .env 并填入您的信息

# --- 必填项 ---
# Let's Encrypt 证书申请邮箱
LE_EMAIL=your-email@example.com
# 您指向本服务器的域名
DOMAIN=your-domain.com

# --- 核心配置 (可选) ---
# 连接密码。如果留空或设为 "changeme"，将自动生成一个16位的强随机密码。
PASSWORD=changeme

# 伪装配置。当服务器被直接访问时，将反向代理到此 URL。留空则默认伪装成 Bing。
MASQUERADE_URL=https://bing.com

# --- 性能与高级配置 (可选) ---
# Hysteria 版本
HYSTERIA_VERSION=2.6.2
# 针对 amd64 架构，是否使用 AVX 指令集版本 (可选值: -avx)。其他架构请留空。
HYSTERIA_ARCH_SUFFIX=-avx

# 是否开启自动测速。设为 "false" 可禁用，并使用下面的手动设置值。
AUTO_SPEEDTEST=true
# 测速带宽折扣率，用于保证稳定性。
SPEED_DISCOUNT=0.8
# 手动设置上传速度 (单位: Mbps)，仅在 AUTO_SPEEDTEST=false 时生效。
UP_MBPS=100
# 手动设置下载速度 (单位: Mbps)，仅在 AUTO_SPEEDTEST=false 时生效。
DOWN_MBPS=100

# 混淆类型 (如: salamander)
OBFS_TYPE=salamander
# 混淆密码。如果留空，将默认使用主密码。
OBFS_PASSWORD=