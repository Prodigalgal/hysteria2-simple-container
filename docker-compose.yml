services:
  traefik:
    image: traefik:v2.11
    container_name: traefik
    restart: unless-stopped
    ports:
      - "443:443/tcp"
      - "127.0.0.1:8080:8080"
    volumes:
      - ./letsencrypt:/letsencrypt
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command:
      - --log.level=INFO
      - --ping=true
      - --api.insecure=true
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --entrypoints.websecure.address=:443/tcp
      - --entrypoints.traefik.address=:8080
      - --certificatesresolvers.le.acme.tlschallenge=true
      - --certificatesresolvers.le.acme.email=${LE_EMAIL}
      - --certificatesresolvers.le.acme.storage=/letsencrypt/acme.json
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping.entrypoint=traefik"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    env_file:
      - .env

  hysteria-server:
    build:
      context: .
      args:
        - HYSTERIA_VERSION=${HYSTERIA_VERSION}
        - HYSTERIA_ARCH_SUFFIX=${HYSTERIA_ARCH_SUFFIX}
    container_name: hysteria-server
    ports:
      - "443:443/udp"
    dns:
      - 8.8.8.8
      - 1.1.1.1
    restart: unless-stopped
    depends_on:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.tcp.routers.hysteria-fallback.rule=HostSNI(`${DOMAIN}`)"
      - "traefik.tcp.routers.hysteria-fallback.entrypoints=websecure"
      - "traefik.tcp.routers.hysteria-fallback.tls.certresolver=le"
      - "traefik.tcp.services.hysteria-fallback.loadbalancer.server.port=443"
    healthcheck:
      test: ["CMD-SHELL", "netstat -uln | grep -q :443 || exit 1"]
      interval: 20s
      timeout: 10s
      retries: 3
      start_period: 40s
    volumes:
      - ./entrypoint.sh:/usr/local/bin/entrypoint.sh:ro
      - ./letsencrypt:/etc/hysteria/certs:ro
    env_file:
      - .env